# What is this for?

This docker compose set up a primary replica using pull-replication to replicate the data
over git http across the 2 nodes.

Just spin up the docker compose to run it: `docker-compose up`